{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "A simple Node.js app to test with Jenkins",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "jest-junit": "^15.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./",
    "outputName": "test-results.xml"
  }
}
